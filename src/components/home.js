import React, { Component } from 'react';

class Header extends Component {
  render() {
    var trstyles={
        color: '#FF0000'
    }
    return (
        <header style = {trstyles}>header</header>
    );
  }
}

export default Header;
